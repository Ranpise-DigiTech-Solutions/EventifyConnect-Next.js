import React from 'react'

type Props = {
    open: boolean;
    handleClose: ()=> void;
}

const UserRegistrationFormComponent = ({ open, handleClose }: Props) => {
  return (
    <div>UserRegistrationFormComponent</div>
  )
}

export default UserRegistrationFormComponent